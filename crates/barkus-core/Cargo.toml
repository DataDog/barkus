[package]
name = "barkus-core"
version = "0.1.0"
edition = "2021"

[dependencies]
rand = "0.10"
serde = { version = "1", features = ["derive"] }
serde_json = "1"

[dev-dependencies]
criterion = { version = "0.5", features = ["html_reports"] }

[[bench]]
name = "core_benchmarks"
harness = false
