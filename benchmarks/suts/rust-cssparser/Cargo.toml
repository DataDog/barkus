# Stub parent crate so `cargo fuzz` finds the workspace root.
# The real harnesses live in fuzz/ as individual binaries.
[package]
name = "rust-cssparser-sut"
version = "0.0.0"
edition = "2021"
publish = false

[lib]
path = "src/lib.rs"

[workspace]
members = ["fuzz"]
exclude = ["../../../target"]
